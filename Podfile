# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Substitute Payment' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Substitute Payment
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  target 'Substitute PaymentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Substitute PaymentUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
